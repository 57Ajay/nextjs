/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains :["unsplash.com", "images.unsplash.com", "images.app.goo.gl", "fakestoreapi.com"]
    }
};

export default nextConfig;
